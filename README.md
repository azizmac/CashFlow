# CashFlow AI

Self-hosted приложение для анализа личных и предпринимательских финансов: куда уходят деньги, кто прислал, кому ушло. **Только чтение** — никаких платежей и переводов.

## Что умеет (MVP)

- Импорт выписок (проверено на реальных файлах, суммы сходятся с итогами банка):
  - **СберБизнес / СберКазначейство** (р/с ИП/ЮЛ): «Экспорт в 1С» (txt, 1CClientBankExchange) — контрагенты, ИНН, назначение платежа, магазины по бизнес-карте; «Операции → Экспорт в XLSX» — контрагенты и назначение без ИНН; «Выписка операций по лицевому счёту» XLSX/PDF (краткая, без контрагентов) и расширенная XLSX (с контрагентами). Любой из форматов за тот же период дополняет уже загруженные операции, а не дублирует их; контрагенты-заглушки вроде «Организация (поступление) · 0123» переименовываются в настоящие названия.
  - **Сбер физлицо**: PDF «Выписка по счёту дебетовой карты» из СберБанк Онлайн (код авторизации → стабильный ID, MCC из описания).
  - **Т-Банк физлицо**: выгрузка операций XLSX/CSV из личного кабинета.
  - Любой банк с экспортом в 1С (Т-Бизнес, Альфа, ВТБ…) — банк определяется по содержимому файла.
- API-подключения (read-only): **T-Invest API** (брокерские счета и ИИС), **T-API** (р/с ИП/ЮЛ в Т-Бизнесе), **Sber API** (р/с в СберБизнесе, mTLS + OAuth2), **Alfa API** (р/с в Альфа-Бизнесе).
- Подключение «через авторизацию в банке» (OAuth 2.0 + PKCE): СберБизнес ID и Alfa ID — пользователь входит на странице банка, токены получает сервер. См. раздел «Интеграции через OAuth».
- Контрагенты: автоматическое сопоставление по ИНН, счёту, телефону СБП, названию; свои счета распознаются как «Я».
- Переводы между своими счетами исключаются из доходов/расходов.
- Категоризация: правила (системные + свои), MCC, обучение на ручных исправлениях. Ручная категория никогда не перетирается.
- Профили **физлицо / ИП** у одного пользователя — раздельно и сводно.
- Многопользовательский: регистрация, вход, у каждого свои данные.

## Быстрый старт (Docker)

```bash
cp .env.example .env
# задать POSTGRES_PASSWORD и ENCRYPTION_MASTER_KEY (openssl rand -base64 32)
docker compose up -d --build
# http://localhost:8080 → Регистрация
```

Миграции и справочники применяются автоматически при старте.

## Разработка

Требуется .NET 9 SDK и PostgreSQL (локально или `docker compose up db`).

```bash
dotnet test
cd src/CashFlow.Web && dotnet run     # appsettings.Development.json содержит dev-ключ шифрования
```

Новая миграция: `cd src/CashFlow.Infrastructure && dotnet ef migrations add <Name> -o Persistence/Migrations`.

## Безопасность

- Все банковские API вызываются с вашего сервера; данные никуда не отправляются.
- Токены/сертификаты, ИНН, номера счетов, телефоны, сырые выписки шифруются **AES-256-GCM** на уровне колонок. Ключ — из `ENCRYPTION_MASTER_KEY` (env), в БД не хранится. Потеря ключа = потеря зашифрованных полей.
- Коннекторы не содержат методов записи (`IConnector` — только `Get*`). Токены запрашивайте у банка с минимальными правами: T-Invest — «только чтение», T-API — «счета и выписки», Sber API — только scope выписок.
- Пароли: ASP.NET Identity (PBKDF2), блокировка после 5 неудачных попыток.

## Структура

```
src/
  CashFlow.Domain                   сущности: Account, Transaction, Counterparty, Category, правила; сервисы матчинга
  CashFlow.Application              импорт (дедуп → контрагенты → переводы → категоризация), синк, сидинг
  CashFlow.Connectors.Abstractions  IConnector (read-only), IStatementParser, External* DTO
  CashFlow.Connectors.Statements    парсеры: SberPdfStatementParser, TBankOperationsParser
  CashFlow.Connectors.TInvest       gRPC, Tinkoff.InvestApi SDK
  CashFlow.Connectors.TBank.Business  REST /api/v4/bank-accounts, /api/v1/statement
  CashFlow.Connectors.Sber.Business   REST + mTLS + OAuth2 refresh_token (с ротацией)
  CashFlow.Infrastructure           EF Core + Npgsql, шифрование колонок, ISecretStore, миграции
  CashFlow.Web                      Blazor Server: обзор, операции, контрагенты, счета, импорт, подключения, категории, профили
tests/CashFlow.Domain.Tests         xUnit
docs/                               исследование API банков (bank-integrations.md, banks/ — карточки и MCP)
```

## Статус и ограничения

- Парсер Сбер PDF написан по типовой структуре выписки СберБанк Онлайн и требует проверки на реальных файлах (пишите issue с обезличенным примером).
- T-API и Sber API коннекторы реализованы по документации, без прогона на боевых учётках; JSON-поля читаются лениво с запасными именами.
- Open API ЦБ РФ (стандарт для физлиц, 2027+) — в планах, заготовка `ConnectorType.CbrOpenApi`.
- Локальный ИИ-категоризатор (Ollama) — следующий этап; сейчас правила + MCC.

## Лицензия

MIT

## Архитектура и проекты

В `CashFlow.sln` проекты разложены по solution-папкам по слоям: `src/1 Core` (Domain, Application), `src/2 Infrastructure` (+ `Connectors`), `src/3 Server` (Api, Server), `src/4 Client` (UI, Client), `src/5 Hosts` (Web, Maui), `tests`, `tools`. Номера задают порядок чтения сверху вниз: зависимости направлены от хостов к ядру.

```
src/CashFlow.Domain            сущности, value objects, доменные сервисы (матчинг контрагентов, переводов, правила)
src/CashFlow.Application       use cases и единственная граница для UI/API: DTO (Contracts), интерфейсы (IProfileService, ILedgerQueries,
                               ILedgerCommands, ICategoryService, IImportService, IConnectionsService) и их серверные реализации
src/CashFlow.Infrastructure    EF Core + PostgreSQL, шифрование полей, хранилище секретов, планировщик синхронизации
src/CashFlow.Connectors.*      парсеры выписок и API банков (только чтение)
src/CashFlow.Api               REST /api поверх интерфейсов Application; вход по bearer-токену Identity (/api/auth)
src/CashFlow.Server            composition root сервера: Identity, PostgreSQL, парсеры, коннекторы, REST — общий для веба и настольного приложения
src/CashFlow.UI                общие Razor-страницы и макет; знают только DTO и две абстракции хоста (ICurrentUser, IAppShell)
src/CashFlow.Web               сервер: Blazor Server (тот же UI, вход по cookie) + API + OAuth-callback банков
src/CashFlow.Client            HTTP-реализации интерфейсов Application для клиентов
src/CashFlow.Maui              MAUI Blazor Hybrid: тот же UI, данные через CashFlow.Client → сервер
tools/StatementProbe           проверка парсеров на файлах и сквозной прогон импорта через PostgreSQL
tools/docker-mcp               MCP-сервер поверх docker CLI для Claude Code
```

Правило безопасности: наружу (в страницы, API, MAUI) уходят только DTO. В них нет ссылок на секреты, номера счетов и телефоны маскированы, сырая запись выписки отдаётся только в карточке одной операции. Сервер берёт пользователя из cookie или токена и проверяет владельца в каждой команде, поэтому клиент не может запросить чужие данные.

## Мобильное и настольное приложение (MAUI)

**Настольная «общая сборка» (Windows, macOS).** Сервер CashFlow встроен в приложение: при запуске оно само поднимает API на `http://127.0.0.1:47831` (порт настраивается), применяет миграции и подключается к нему — адрес сервера вводить не нужно, только e-mail и пароль. Нужен лишь PostgreSQL: по умолчанию тот, что поднимает `docker compose up -d db` (проброшен на `127.0.0.1:55432`, см. `DB_PORT` в `.env`).
Настройки лежат в `server.json` в папке данных приложения (Windows: `%LOCALAPPDATA%\<User>
u.cashflow.app\Data\server\`), рядом — `server.log` с ошибками сервера. При первом запуске файл создаётся автоматически: строка подключения и ключ шифрования берутся из переменных окружения или из `.env` рядом с `docker-compose.yml` (если exe запущен из папки репозитория), иначе ключ генерируется. Если приложение и веб-контейнер работают с одной базой, ключ `ENCRYPTION_MASTER_KEY` должен совпадать. Если база недоступна, экран входа показывает причину и форму с параметрами подключения. Ссылка «Подключиться к другому серверу CashFlow» переключает приложение в режим клиента (как на телефоне). Общая точка сборки сервера для веба и приложения — проект `CashFlow.Server`.

**Сообщения об ошибках** приходят на русском: тексты Identity (пароль, e-mail, блокировка) заданы в `RussianIdentityErrorDescriber`, сетевые и HTTP-ошибки переводит `CashFlow.Client.ApiClient`.


`src/CashFlow.Maui` — один код для Windows, macOS, iOS и Android. Приложение не хранит данные, оно входит на ваш сервер CashFlow (адрес, e-mail, пароль) и работает через REST; сессия лежит в защищённом хранилище платформы. Экран входа принимает адрес вида `http://192.168.1.10:8080` (домашняя сеть, Tailscale) или `https://cashflow.example.com`.

```bash
# Windows (воркод maui-windows уже есть)
dotnet build src/CashFlow.Maui -f net9.0-windows10.0.19041.0
# Android / iOS / macOS: один раз установить воркоды, затем собирать с мобильными целями
dotnet workload install maui
dotnet build src/CashFlow.Maui -p:MobileTargets=true -f net9.0-android
```

Что нужно для распространения: Android ставится из APK, iOS требует аккаунт разработчика Apple и TestFlight, macOS — подпись и нотаризацию. Подключение банков «через авторизацию» из приложения открывает системный браузер на сервере: там нужен вход в веб-версию.

## Дизайн и страницы

Интерфейс собран по макетам Claude Design (`docs/design/cashflow-ai-ui-mockups/project/CashFlow AI.dc.html`): «сдержанное жидкое стекло», iPhone-first, тот же код — на десктопе и в MAUI.

- **Темы.** Тёмная — базовая, светлая — через `[data-theme=light]`, «Система» следует `prefers-color-scheme`. Выбор хранится в `localStorage` (работает и в WebView MAUI), ставится inline-скриптом до первой отрисовки, переключается в Настройках (`ThemeService`). Все цвета — CSS-переменные в `src/CashFlow.UI/wwwroot/app.css`.
- **Навигация.** Шире 900px — боковая панель 216px (Обзор, Операции, Категории, Контрагенты, Счета, Импорт, Подключения, Настройки). Уже — плавающий таббар: Обзор · Операции · ⇪ (загрузить выписку) · Счета · Ещё.
- **Обзор** — переключатель Месяц/3 месяца/Год, карточка Net worth с доходами, расходами и сальдо, insight-карточка, «Needs review» (операции без категории), «Куда уходят деньги» с иконками и полосами долей, «Кому уходят деньги» с аватарами.
- **Операции** — чипы Все / Без категории / Переводы / Доходы / Расходы, поиск, группировка по дням, иконка категории в строке, предложение категории с кнопкой ✓, смена категории на месте.
- **Контрагенты** (`/counterparties`) — поиск, чипы по типу, список «top by spend»; **карточка контрагента** (`/counterparties/{id}`) — аватар, ИНН/КПП/счета, Year / Avg / Trend, столбики за 12 месяцев, категория по умолчанию, операции.
- **Категории** — donut по расходам за период, список с динамикой к прошлому периоду, свои категории и правила.
- **Счета и банки** — Net worth со stacked-bar по типам счетов, группы по банкам, флаги «поток/активы», ручной счёт.
- **Импорт выписки** (`/sources`, `/import`) — dropzone, список файлов, карточки результата разбора («новых операций», период, контрагенты), карточки источников со статусами и «Add source».
- **Подключения** — карточки подключений, «Подключите банк» (OAuth-провайдеры, если настроены) и форма по токену.
- **Настройки** (`/settings`) — аккаунт, профили с ИНН, Appearance (Тёмная/Светлая/Система), ссылки на смену пароля и 2FA.
- **Вход** — и в вебе (Identity), и в MAUI одинаковая стеклянная форма из макета онбординга.

Экраны макета, для которых нет функционала на сервере (прогноз на 6 месяцев, ИИ-чат ⌘K, Face ID, код восстановления, предложение объединить контрагентов), пока не реализованы.

### Подробнее о ключевых страницах

- **Источники данных** (`/sources`) — одно место для загрузки: зона перетаскивания файлов, карточки покрытия по каждому счёту (за какой период есть операции, откуда они пришли, когда загружались), подсветка счетов, по которым данных нет больше 30 дней, с подсказкой «догрузите с такой-то даты» и инструкцией, где взять выписку в конкретном банке. Здесь же синхронизация API-подключений и кнопки OAuth.
- **Операция** (`/transactions/{id}`) — из списка операций, со счёта или из контрагента можно провалиться в карточку: сумма, категория (с применением ко всем операциям контрагента), реквизиты контрагента из выписки, назначение платежа, парная операция перевода, источник (подключение, файл, внешний ID, сырая запись), заметка и теги, похожие операции.
- **Обзор** показывает баннер, если по какому-то счёту данные устарели.

## Механика выписок и подключений

- **Подключение** (`Connection`) для файлов создаётся на каждую комбинацию *профиль × банк × формат* (`SourceCode` = код парсера: `sber-card-pdf`, `sber-business`, `1c-client-bank`, `tbank-operations`). PDF по карте Сбера и 1С-выгрузка р/с СберБизнеса — разные подключения со своей историей импортов; на странице импорта видно, в какое подключение и счёт попал файл.
- **Счёт** (`Account`) ищется по всему профилю и банку по полному 20-значному номеру, а не только внутри подключения: XLSX, 1С и Sber API по одному р/с попадают в один счёт. API-подключение «усыновляет» счёт, созданный из выписки, когда находит тот же номер.
- **Дедупликация** — по внешнему ID (номер документа / код авторизации / uuid из API) или по хэшу (счёт, дата, сумма, описание). Повторная загрузка того же периода безопасна. У р/с Сбера ключ общий для XLSX, PDF и 1С (дата, номер документа, сумма, корр. счёт): один и тот же документ из разных форматов — одна операция.
- **Обогащение** — если после краткой XLSX загрузить 1С, операции не дублируются, а получают контрагента с ИНН, назначение платежа и магазин по бизнес-карте; контрагенты пересопоставляются. Проверено на реальных файлах: `dotnet run --project tools/StatementProbe -- --db "<строка подключения PostgreSQL>" файлы…` импортирует их дважды и печатает сводку (подключения, счета, дубликаты).
- **Банк файла 1С** определяется по полю «Отправитель» и банку своей стороны (по названию или БИК), поэтому выгрузки Т-Бизнеса/Альфы/ВТБ ложатся в нужный банк.
- Если несколько парсеров узнают файл, берётся тот, что вернул операции.

Проверить, как читается конкретный файл, не запуская приложение:

```bash
dotnet run --project tools/StatementProbe -- "путь/к/выписке.pdf" --raw
```

## Интеграции через OAuth (подключение «через авторизацию»)

Мультибанк, который вы видели у Т-Банка (счета Сбера/ВТБ/Альфы в приложении Т-Банка), работает по Открытым API Банка России в пилоте между банками; стороннему self-hosted приложению этот канал пока недоступен (см. `docs/bank-integrations.md`). Что доступно: **прямая интеграция** ИП/ЮЛ со своим же банком по OAuth 2.0 Authorization Code + PKCE.

1. Зарегистрируйте приложение в ЛК банка и укажите redirect URI:
   - Сбер: СберБизнес → Интеграции → Sber API (прямая интеграция), redirect `https://<ваш адрес>/oauth/sberbusiness/callback`, права только «Информация о счетах» и «Выписки», выпустите клиентский сертификат (.pfx).
   - Альфа: developers.alfabank.ru → Alfa API, redirect `https://<ваш адрес>/oauth/alfabusiness/callback`.
2. Заполните в `.env`: `PUBLIC_BASE_URL`, `SBER_CLIENT_ID`, `SBER_CLIENT_SECRET`, `SBER_CERT_PFX_PATH` (файл кладётся в `./secrets/`), `SBER_CERT_PASSWORD`, `ALFA_CLIENT_ID`, `ALFA_CLIENT_SECRET`. Для локальной разработки — те же ключи в `appsettings.Development.json`, секция `Integrations`.
3. На странице «Подключения» появится кнопка «Подключить через СберБизнес ID / Alfa ID». После входа в банке подключение создаётся автоматически и сразу синхронизируется.

URL авторизации и методов Alfa API заданы в конфигурации (`Integrations:Alfa:*`) и требуют сверки с актуальной инструкцией банка. Для Т-Бизнеса основной путь — самостоятельный токен из ЛК; партнёрский OAuth через T-Business ID (`id.tbank.ru/auth/authorize`, `/auth/token`) тоже реализован и включается ключами `TBANK_CLIENT_ID` / `TBANK_CLIENT_SECRET`, но банк выдаёт партнёрский доступ по заявке на tid@tbank.ru от 30 тыс. авторизаций в месяц.

## Docker MCP

В `.mcp.json` подключён MCP-сервер `tools/docker-mcp/server.mjs` (Node, без зависимостей): инструменты `docker` (любая команда CLI), `docker_compose` (в корне репозитория) и `docker_overview`. Через него Claude Code видит контейнеры и образы, собирает и перезапускает `docker compose`. Каталожный сервер Docker MCP Toolkit на Windows не передаёт аргументы в контейнер, поэтому используется свой.
